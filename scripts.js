// This is an example of a JavaScript function that would be included in a separate script.js file and linked to the HTML file.

function toggleMenu() {
  const nav = document.querySelector('nav');
  nav.classList.toggle('active');
}
